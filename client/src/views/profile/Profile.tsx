import HeaderProfile from './components/HeaderProfile';
import MainProfile from './components/MainProfile';

const Profile = () => {

  return (
    <div className=' py-[30px] px-[20px] h-auto bg-white dark:bg-black'>
      <HeaderProfile />
      <MainProfile />
    </div>
  );
};

export default Profile;
